{
  "name": "glock-client",
  "version": "0.0.1",
  "description": "Glock is a Browser/Node.js library that allows you to transport AV stream over UDP to a server via WebRTC.",
  "repository": {
    "type": "git",
    "url": "https://github.com/castoapp/glock"
  },
  "author": "casto",
  "type": "module",
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "serve": "http-server .",
    "publish": "npm run build && npm publish"
  },
  "dependencies": {},
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
